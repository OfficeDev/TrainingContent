<Page
    x:Class="O365Universal.ItemDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:O365Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock x:Name="pageTitle" Style="{StaticResource PageHeaderTextStyle}" Margin="10,5,10,5" FontSize="36" Text="foo"/>
        </Grid>
        <Grid Grid.Row="1">
            <FlipView Name="imgFlipView" ItemsSource="{Binding}" SelectionChanged="imgFlipView_SelectionChanged">
                <FlipView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <local:WaitingIndicator x:Name="waiting"></local:WaitingIndicator>
                            <Image Source="{Binding Bitmap}"></Image>
                        </Grid>
                    </DataTemplate>
                </FlipView.ItemTemplate>
            </FlipView>
        </Grid>
    </Grid>
</Page>
